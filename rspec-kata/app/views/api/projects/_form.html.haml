= form_for @api_project do |f|
  - if @api_project.errors.any?
    #error_explanation
      %h2= "#{pluralize(@api_project.errors.count, "error")} prohibited this api_project from being saved:"
      %ul
        - @api_project.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
